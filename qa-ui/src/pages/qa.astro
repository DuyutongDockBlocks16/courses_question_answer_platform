---
// src/pages/qa.astro
import Layout from "../layout/Layout.astro";
import QaPlatform from "../components/QaPlatform.svelte";

// 获取查询参数
const { searchParams } = new URL(Astro.request.url); // 确保使用 Astro.request.url
const courseId = searchParams.get("courseId");
const userUuid = searchParams.get("userUuid");

console.log("URL(Astro.request.url)", Astro.request.url);
console.log("courseId", courseId);
console.log("userUuid", userUuid);
---

<Layout>
  <QaPlatform client:only={"svelte"} courseId={courseId} userUuid={userUuid} />
</Layout>


